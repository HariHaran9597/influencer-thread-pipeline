# Core graph framework
langgraph>=0.2,<0.7
langchain-core>=0.3

# Live mode (all free tiers) — pipeline auto-falls back to mock mode without keys
langchain-groq>=0.2        # GROQ_API_KEY  — writer/editor LLM (free tier: ~30 req/min, 14.4k req/day)
tavily-python>=0.5         # TAVILY_API_KEY — web research (free tier: 1,000 credits/month)

# UI + eval
gradio>=4.44
python-dotenv>=1.0
pytest>=8.0

# API backend for the custom web frontend (Vercel frontend + this on HF Spaces)
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7

# Optional extras (uncomment if you want them):
# langchain-google-genai   # GOOGLE_API_KEY — independent judge model (recommended, free)
# langsmith                # LANGSMITH_API_KEY set in .env enables tracing (5k free traces/mo)
# sentence-transformers    # local BGE cross-encoder fact-check scoring (no API needed)
